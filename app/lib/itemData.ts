export interface ItemEffects {effects:{emoji:string;label:string}[];description:string;metaDescription:string;consume:string;}
const notes=[{emoji:"*",label:"Menu Listed"},{emoji:"+",label:"Compare Details"},{emoji:"i",label:"Ask Staff"}];
export function getItemData(category:string,name:string):ItemEffects{const label=category.toLowerCase().replaceAll("_"," ");return{effects:notes,description:`${name} appears in the ${label} category at King Rock. Review the current menu and package details before visiting.`,metaDescription:`Review ${name} menu information at King Rock in Toronto. Check current details before visiting.`,consume:"Review the package and current menu details. Ask staff if an item detail is unclear."};}
